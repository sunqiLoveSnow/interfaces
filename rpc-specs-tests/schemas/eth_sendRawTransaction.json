{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "eth_sendRawTransaction",
    "title": "eth_sendRawTransaction",
    "description": "eth_sendRawTransaction JSON-RPC method request and response schema.",

    "request": {
      "id": "#request",
      "allOf": [
        { "$ref": "jsonrpc-request.json" },
        { "$ref": "#/definitions/request-obj" }
      ],
      "definitions": {
        "request-obj": {
          "id": "#request-obj",
          "properties": {
            "method": {
              "type": "string",
              "enum": ["eth_sendRawTransaction"]
            },
            "params": {
              "type": "array",
              "items": [
                {
                  "type": "string",
                  "description": "DATA, The signed transaction data."
                }
              ]
            }
          }
        }
      }
    },


    "response": {
      "id": "#response",
      "allOf": [
        { "$ref": "jsonrpc-response.json" },
        { "$ref": "#/definitions/response-obj" }
      ],
      "definitions": {
        "response-obj": {
          "id": "#response-obj",
          "properties": {
        		"result": {
                    "type": "string",
                    "description": "DATA, 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available."
        		}
        	}
        }
      }
    }

}
